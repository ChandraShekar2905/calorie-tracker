# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v54.0.0/ before writing any code.
(Project is pinned to SDK 54 because the phone used for demos can only run an Expo Go version that supports SDK 54.)
